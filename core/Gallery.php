<?php
	/* Generated by Wixnit Class Builder 
	// Sep, 02/2019
	// Building class for Gallery
	*/

	class Gallery
	{
		public $Id = "";
		public $Created = 0;
		public $Heading = "";
		public $Description = "";
		public $Status = false;
		public $Image = "";
		public $Sort = 0;
		
		private $subscriber = null;

		function __construct(Subscriber $subscriber)
		{
			$this->subscriber = $subscriber;
		}
		
		public function Initialize($arg=null)
		{
			if($arg != null)
			{
				$db = $this->subscriber->GetDB();

				$res = $db->query("SELECT * FROM gallery WHERE galleryid='$arg'");

				if($res->num_rows > 0)
				{
					$row = $res->fetch_assoc();
				
					$this->Id = $row['galleryid'];
					$this->Created = new WixDate($row['created']);
					$this->Heading = $row['heading'];
					$this->Description = $row['description'];
					$this->Status = Convert::ToBool($row['status']);
					$this->Image = $row['image'];
					$this->Sort = $row['sort'];
				}
			}
		}

		public function Save()
		{
			$db = $this->subscriber->GetDB();

			$id = $this->Id;
			$created = time();
			$heading = addslashes($this->Heading);
			$description = addslashes($this->Description);
			$status = Convert::ToInt($this->Status);
			$image = addslashes($this->Image);
			$sort = Convert::ToInt($this->Sort);

			if($res = $db->query("SELECT galleryid FROM gallery WHERE galleryid='$id'")->num_rows > 0)
			{
				$db->query("UPDATE gallery SET heading='$heading',description='$description',status='$status',image='$image',sort='$sort' WHERE galleryid = '$id'");
			}
			else
			{
				redo: ;
				$id = Random::GenerateId(16);
				if($db->query("SELECT galleryid FROM gallery WHERE galleryid='$id'")->num_rows > 0)
				{
					goto redo;
				}
				$this->Id = $id;
				$db->query("INSERT INTO gallery(galleryid,created,heading,description,status,image,sort) VALUES ('$id','$created','$heading','$description','$status','$image','$sort')");
			}
		}

		public function Delete()
		{
			$db = $this->subscriber->GetDB();

			$id = $this->Id;
			$db->query("DELETE FROM gallery WHERE galleryid='$id'");
		}

		public static function Search(Subscriber $subscriber, $term='')
		{
			$db = $subscriber->GetDB();
			$ret = array();
			$i = 0;

			$res = $db->query("SELECT galleryid FROM gallery WHERE heading LIKE '%$term%' OR description LIKE '%$term%' OR status LIKE '%$term%' OR image LIKE '%$term%' OR sort LIKE '%$term%'");
			while(($row = $res->fetch_assoc()) != null)
			{
				$ret[$i] = new Gallery($row['galleryid']);
				$i++;
			}
			return $ret;
		}

		public static function Filter(Subscriber $subscriber, $term='', $field='galleryid')
		{
			$db = $subscriber->GetDB();
			$ret = array();
			$i = 0;

			$res = $db->query("SELECT galleryid FROM gallery WHERE ".$field." ='$term'");
			while(($row = $res->fetch_assoc()) != null)
			{
				$ret[$i] = new Gallery($row['galleryid']);
				$i++;
			}
			return $ret;
		}

		public static function Order(Subscriber $subscriber, $field='id', $order='DESC')
		{
			$db = $subscriber->GetDB();
			$ret = array();
			$i = 0;

			$res = $db->query("SELECT galleryid FROM gallery ORDER BY ".$field." ".$order."");
			while(($row = $res->fetch_assoc()) != null)
			{
        $ret[$i] = new Gallery($subscriber);
        $ret[$i]->Initialize($row['galleryid']);
				$i++;
			}
			return $ret;
		}

		public static function All(Subscriber $subscriber)
		{
			$db = $subscriber->GetDB();
			$ret = array();
			$i = 0;

			$res = $db->query("SELECT * FROM gallery");
			while(($row = $res->fetch_assoc()) != null)
			{
				$ret[$i] = new Gallery($subscriber);
				$ret[$i]->Id = $row['galleryid'];
				$ret[$i]->Created = new WixDate($row['created']);
				$ret[$i]->Heading = $row['heading'];
				$ret[$i]->Description = $row['description'];
				$ret[$i]->Status = Convert::ToBool($row['status']);
				$ret[$i]->Image = $row['image'];
				$ret[$i]->Sort = $row['sort'];
				$i++;
			}
			return $ret;
		}
	}
